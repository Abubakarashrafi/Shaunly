import { Github } from "lucide-react";

const Navbar = () => {
    return (
        <nav className="w-full px-6 py-4 flex justify-between border-b items-center bg-black text-white shadow-md">
            <h1 className="text-3xl font-bold">Shaunly</h1>

            
        </nav>
    );
};

export default Navbar;
